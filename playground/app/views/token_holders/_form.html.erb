<%= form_for(token_holder) do |f| %>
  <% if token_holder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(token_holder.errors.count, "error") %> prohibited this token_holder from being saved:</h2>

      <ul>
      <% token_holder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :token %>
    <%= f.text_field :token %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
